# Git Workflow Demo Notes

这份文件用于 Day 5 课堂中的真实 Git 日志演示。

演示顺序：

1. `git add` 选择进入提交的文件。
2. `git diff --cached` 检查暂存区。
3. `git commit` 在本地仓库创建提交。
4. `git status -sb` 显示本地分支领先远程。
5. `git push` 把本地提交发送到 GitHub。

仓库地址：https://github.com/Tauh8/ai-
